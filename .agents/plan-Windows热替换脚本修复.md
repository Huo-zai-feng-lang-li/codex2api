# Windows 热替换脚本修复

## 最终目标
- 服务继续运行期间先完成前端和临时后端二进制构建。
- 仅在 `responses_memory.inflight_requests == 0` 后强杀旧进程并替换。
- 新服务健康检查失败时恢复旧二进制并重新启动，命令返回失败。

## 影响范围
- `build-and-restart.bat`
- `scripts/build-and-restart.test.mjs`
- `.agent/rules/README.md`

## 阶段状态
1. [x] 取证：确认旧脚本先强杀、直接覆盖 EXE、健康失败仍返回成功。
2. [x] RED：4/4 契约测试按预期失败，锁定先构建、排空、备份、回滚与明确退出码契约。
3. [x] GREEN：实现服务在线构建临时 EXE、排空后强杀替换、健康失败回滚。
4. [x] 验证：批处理契约与可见轮询行为测试通过，61/61 Node 测试、TypeScript 类型检查与 `go test ./... -count=1` 通过；真实成功替换退出码 0。另完成两条故障注入：缺失构建工具时退出码 1 且旧 PID/EXE 不变；新服务启动后被终止时触发回滚，脚本退出码 1、旧 EXE 哈希恢复、旧服务重新健康。最终服务 PID `22816`、健康状态 `ok`，EXE SHA256 `EB4CA755B75394425A1895D7D9BD76C1F8089F21353BD90129C54F6502919AD5`，无临时 EXE/PID 文件残留。
5. [x] 提交准备：独立审查、故障注入与最终验证完成，本计划随最终提交归档。

## 已知边界
- 当前服务没有独立的本地 drain/readiness 控制面；脚本已绑定项目路径、监听端口和精确 PID，并在读取 `inflight_requests == 0` 后立即终止旧进程，但检查与终止之间仍存在极窄的新请求进入窗口。增加无鉴权控制端点会扩大安全面，本次保持本地热替换的最小边界。
